#include "main.h"
#include <stdlib.h>

/**
* _abs - Entry point
* @n: value passed to function to find absolute
* Description: function that computes the absolute value of an integer
* Return: absolute of n
*/

int _abs(int n)
{
return (abs(n));
}
