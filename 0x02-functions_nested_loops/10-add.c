#include "main.h"

/**
* add - Entry point
* @a: first variable
* @b: second variable
* Description: function that adds two integers and returns the result
* Return: sum of a and b
*/

int add(int a, int b)
{
return (a + b);
}
