#include <stdio.h>

/**
* mod - Entry point
* @a: first variable
* @b: second variable
* Description: Finds modulus of 2 integers
* Return: Modulus of a and b
*/

int mod(int a, int b)
{
	return (a % b);
}
