#include <stdio.h>

/**
* mul - Entry point
* @a: first variable
* @b: second variable
* Description: Finds product of 2 integers
* Return: Product of a and b
*/

int mul(int a, int b)
{
	return (a * b);
}
