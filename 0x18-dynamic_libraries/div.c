#include <stdio.h>

/**
* div - Entry point
* @a: first variable
* @b: second variable
* Description: Finds quotient of 2 integers
* Return: Quotient of a and b
*/

int div(int a, int b)
{
	return (a / b);
}
