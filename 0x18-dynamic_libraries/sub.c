#include <stdio.h>

/**
* sub - Entry point
* @a: first variable
* @b: second variable
* Description: Subtracts two integers and returns the result
* Return: Difference a and b
*/

int sub(int a, int b)
{
	return (a - b);
}
