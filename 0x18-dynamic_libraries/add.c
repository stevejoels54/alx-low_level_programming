#include <stdio.h>

/**
* add - Entry point
* @a: first variable
* @b: second variable
* Description: Adds two integers and returns the result
* Return: sum of a and b
*/

int add(int a, int b)
{
	return (a + b);
}
